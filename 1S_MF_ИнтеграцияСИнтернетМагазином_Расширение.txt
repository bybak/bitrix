// =============================================================================
// ГЛАВНЫЙ ФАЙЛ ДЛЯ УНФ — хуки в ИнтеграцияСИнтернетМагазиномСервер (v14.8)
//
// КУДА: расширение → заимствовать ИнтеграцияСИнтернетМагазиномСервер → вставить → F7
// БЕЗ ЭТОГО ФАЙЛА проведение и PayKeeper НЕ РАБОТАЮТ (подписки не хватает).
//
// Хук 3 — при повторном обмене (ранний Возврат) отмена + дедуп.
// Хук 5 — проведение для НОВЫХ заказов + кэш отмены из XML.
// Хук 6 — запасной путь при ПОВТОРНОМ обмене (когда заказ уже в базе).
// =============================================================================

// 1) 36-382 вместо CA-036382.
&Вместо("ПолучитьНомерДокументаССайта")
Функция MF_ВместоПолучитьНомерДокументаССайта(Знач НомерНаСайте, Организация, Параметры)

	Возврат MF_NumbersOrdersSaitServer.MF_НомерДокументаВместоОбмена(НомерНаСайте);

КонецФункции


// 2) Запомнить номер из XML + отмена с сайта (даже если заказ уже загружен).
&После("ЗаполнитьЗаказДаннымиДокументаXDTO")
Процедура MF_ПослеЗаполнитьЗаказДаннымиДокументаXDTO(ДокументОбъект, ДокументXDTO, СвойстваЗаказа, Параметры, ОписаниеОшибки)

	MF_NumbersOrdersSaitServer.MF_ЗапомнитьНомерСайтаИзОбмена(ДокументОбъект, ДокументXDTO, СвойстваЗаказа);
	MF_NumbersOrdersSaitServer.MF_ПроверитьОтменуЗаказаИзОбмена(ДокументОбъект, ДокументXDTO, СвойстваЗаказа);

КонецПроцедуры


// 3) Номер 36-XXX, без CA-*. При пропуске записи — отмена из кэша XML (хук2/5).
&Вместо("ЗаписатьДокумент")
Процедура MF_ВместоЗаписатьДокумент(Параметры, ДокументОбъект, ЗаписыватьВсегда = Ложь)

	Если Не ЗаписыватьВсегда Тогда
		Если (Не ДокументОбъект.Ссылка.Пустая() И НЕ ОбновлятьЗагруженныеДокументы(Параметры, ДокументОбъект.Проведен)) Тогда
			MF_NumbersOrdersSaitServer.MF_ПрименитьОтменуПриПропускеЗаписиОбмена(ДокументОбъект);
			Возврат;
		КонецЕсли;
	КонецЕсли;

	MF_NumbersOrdersSaitServer.MF_ЗаписатьДокументОбменаВместо(ДокументОбъект);

	Если НЕ ЗначениеЗаполнено(ДокументОбъект.Дата) Тогда
		ДокументОбъект.Дата = ТекущаяДатаСеанса();
	КонецЕсли;

КонецПроцедуры


// 4) Не плодить второй заказ 36-XXX.
&После("ПолучитьРанееЗагруженныеДокументы")
Процедура MF_ПослеПолучитьРанееЗагруженныеДокументы(МассивНомеров, Результат)

	MF_NumbersOrdersSaitServer.MF_ДополнитьРанееЗагруженныеЗаказы(МассивНомеров, Результат);

КонецПроцедуры


// 5) ГЛАВНЫЙ: после регистра — проведение + PayKeeper (один раз). Кэш отмены из XML.
&Вместо("ЗаписатьЗаказыПокупателейССайта")
Процедура MF_ВместоЗаписатьЗаказыПокупателейССайта(ДокументXDTO, ДокументОбъект, Параметры)

	MF_NumbersOrdersSaitServer.MF_ЗапомнитьОтменуИзXDTO(ДокументXDTO, ДокументОбъект);

	ПродолжитьВызов(ДокументXDTO, ДокументОбъект, Параметры);

	MF_NumbersOrdersSaitServer.MF_ЗавершитьЗаказПослеОбмена(ДокументXDTO, ДокументОбъект);

КонецПроцедуры


// 6) Кэш оплаты из XML (без проведения — иначе ломает обмен).
&После("ОбновитьСоздатьДополнительныеСведенияЗаказа")
Процедура MF_ПослеОбновитьДопСведенияЗаказа(ЗаказПокупателя, СвойстваЗаказа, ОписаниеОшибки, Результат)

	MF_NumbersOrdersSaitServer.MF_ПослеСвойствЗаказаОбмена(ЗаказПокупателя, СвойстваЗаказа);

КонецПроцедуры
