[mysqld]
# Основные настройки
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
default-storage-engine = InnoDB
sql_mode = NO_ENGINE_SUBSTITUTION
; Проверка системы Битрикс (рекомендации продукта).
innodb_strict_mode = OFF
; Чтобы соединение по умолчанию не брало utf8mb4_0900_ai_ci (MySQL 8), а совпадало с БД utf8mb4_unicode_ci.
loose-default_collation_for_utf8mb4 = utf8mb4_unicode_ci

# InnoDB: для каталога Битрикс 512M на боевом сервере обычно мало (частые чтения с диска).
# На ноутбуке с 8 GiB RAM при нехватке памяти уменьшите до 512M или 1G.
innodb_buffer_pool_size = 4G
innodb_redo_log_capacity = 512M
innodb_flush_log_at_trx_commit = 2
innodb_flush_method = O_DIRECT
innodb_io_capacity = 2000
innodb_io_capacity_max = 4000

max_connections = 200
table_open_cache = 4000
table_definition_cache = 2000
max_allowed_packet = 64M

# Оптимизация (MySQL 8: query cache удалён)

# Временные таблицы
tmp_table_size = 64M
max_heap_table_size = 64M

# Логирование
slow_query_log = 1
slow_query_log_file = /var/log/mysql/slow.log
long_query_time = 2

[client]
default-character-set = utf8mb4

[mysql]
default-character-set = utf8mb4

