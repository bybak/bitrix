Снимок папки /local/ на момент копирования — текущий дизайн и логика сайта.
Рабочая копия для редизайна: /local/
Чтобы вернуться к этому варианту: скопировать содержимое этой папки поверх /local/ (с бэкапом нового /local/ при необходимости).
